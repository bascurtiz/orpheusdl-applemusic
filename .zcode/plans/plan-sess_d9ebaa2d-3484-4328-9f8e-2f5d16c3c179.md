Simplification pass on interface.py (3,294 lines → target ~2,300–2,500, i.e. 25–30% reduction) with zero functional change. All edits build on the current working tree (uncommitted fixes preserved).

## Stage 1 — Dead code & imports (mechanical, ~120 lines)
- Delete: `_debug_print`, `_is_aac_stream_codec`, `_is_ssl_certificate_error`, the unreachable second `except` block in `__init__` (effective behavior is already "print, swallow, retry later" — preserved), `is_album_context`/`inspect.stack()` block (keep `indent_spaces` constant), `_using_rich_tagging` attribute, the always-false `'codec_name' in locals()` fallback, `get_track_lyrics`' unreachable `_lazy_import_gamdl` guard.
- Remove unused imports (`requests`, `ssl`, `urllib.request`, `datetime`, `Enum`, `create_temp_filename`, `download_to_temp`) and drop the `OrpheusDownloadEnum` alias by importing `DownloadEnum` explicitly (it currently arrives only via the star import). Add `List` to typing imports explicitly.
- Remove redundant function-local re-imports (`threading`, `re`, `inspect`, `concurrent.futures`, `asyncio`, `subprocess`) by importing once at module level.
- Collapse the 17-line `X = None` global-init block and 17-line `globals()['X'] = X` block in `_lazy_import_gamdl` into one name tuple + `globals().update(...)`.

## Stage 2 — Shared helpers for repeated idioms (~150 lines)
- `self._cookies_path()`: settings path → `./config/cookies.txt` fallback → None; replaces 4 copies.
- `def _first(payload)`: unwrap `{'data': [item]}` → item; replaces 10+ inline copies.
- `self._paged_tracks(tracks_rel)`: pagination via `extend_api_data` + storefront save/restore; replaces the two identical closures/try-finally blocks.
- `self._track_row(track, idx, default_artist, release_year, fallback_cover, extra_attrs=None)`: the track-dict builder shared by `get_album_info`/`get_playlist_info` (album passes label/copyright/upc passthrough via `extra_attrs`).
- Module-level `_label_from_copyright(copyright)` regex helper (verbatim ×2 today).
- Module constant for the wrapper connection-error keyword list (×2 today).
- `_read_orpheus_settings()`: shared `./config/settings.json` reader for `_get_global_lyrics_settings` and `_resolve_all_binary_paths`.
- Unify the ≤48kHz ALAC playlist filter into one helper used by both `OrpheusAppleMusicSongInterface._get_playlist_from_codec_enhanced` and `_get_precise_alac_info` (identical effective condition: alac + LOSSLESS tier).
- Consolidate the two identical exception-translation try/excepts in `get_track_download` (prepare-media and get-download-item) into one small local wrapper; merge `except X: raise` clauses into one `except (AuthenticationError, TrackUnavailableError, DownloadError): raise`.

## Stage 3 — Method-level consolidation (~350 lines), structure-preserving
- `__init__`: use `_cookies_path()`, drop dead except/attrs.
- `_run_async`: keep the 4-attempt/self-healing design; fix the `am_api`/`it_api` copy-paste at 648 (`getattr(it_api, 'storefront', ...)`); tidy debug-print duplication.
- `_quality_to_codec`: collapse the two identical AAC branches.
- `_gamdl_init_failure_message`: single `needs_wrapper` local instead of the repeated `wrapper_requested or self.use_wrapper`.
- `get_track_info`: hoist `_fetch_with_logging` above the raw_result branch (fixes latent NameError, no common-path change); merge its library→catalog core with `_fetch_guest`'s via one shared inner coroutine; remove the duplicated kwargs debug print; drop the second `stream_info` recomputation and use one `wrapper_requested` local.
- `get_track_download`: compute `wrapper_requested` (settings-based) and `wrapper_active` (actual `wrapper_api` state) once each, preserving the distinct fallbacks at 2279/2302/2461; simplify the error-mapping tail using the shared keyword constant.
- `get_album_info`/`get_playlist_info`/`get_artist_info`: rebuild on `_paged_tracks`/`_track_row`; `process_album_item` and the top-songs loop reuse them where semantics match exactly.
- Trim verbose/repetitive comment blocks and redundant debug prints while keeping all load-bearing comments (event-loop rationale, wrapper ports, macOS binary search); error-message strings stay byte-identical.

## Explicitly unchanged
Public API surface (`custom_url_parse`, `search`, `get_track_info`, `get_track_download`, `get_track_lyrics`, `get_track_credits`, `get_track_cover`, `get_album_info`, `get_playlist_info`, `get_artist_info`, `module_information`), all error messages and exception types, retry/backoff constants, the `album_id_from_rels = attrs.get('albumName')` overwrite at 1838 (odd but load-bearing), legacy URL-parse branches, the `_UniversalMock`/`TempPopen` shim, and the recently rewritten `suppress_gamdl_debug`.

## Verification
After each stage: `python -m py_compile interface.py`; AST check that no removed symbol is still referenced; if a sibling OrpheusDL checkout / gamdl install is visible, an import smoke test. Manual spot-check of a diff review at the end. (No test suite exists in this repo.)

Note: two flagged latent bugs get minimal fixes as described (648 one-word fix, fetcher hoist); everything else is strictly behavior-preserving deletion/dedup.